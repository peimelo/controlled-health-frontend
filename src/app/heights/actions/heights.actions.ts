import { createAction } from '@ngrx/store';

export const heightFormDialogDismiss = createAction(
  '[Heights] Height Form Dialog Dismiss'
);

export const loadDashboard = createAction('[Heights] Load Dashboard');
